<h1><%= t(".products") %></h1>
 <% if policy(:product).new?  %>
  <p> <%= link_to t(".add_product"), new_product_path  %>
 <% end %>

<div class="products-list">
  <% @products.each do |product| %>
    <p> <%= link_to product.title, product  %>, <%= product.price %>
    <br>
  <% end %>
</div>
